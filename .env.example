# Backend security and CORS (see SECURITY.md)
API_KEY=your_secure_api_key_here
AUTH_REQUIRED=true
ENVIRONMENT=production
ALLOWED_ORIGINS=http://localhost:7860,http://localhost:5173,https://agents-tool-rag.vercel.app
RATE_LIMIT_PER_MINUTE=60
RATE_LIMIT_PER_HOUR=1000

# Add other backend env vars as needed (Qdrant, Supabase, LLM providers, etc.).
# Do not commit real secrets; copy this file to .env and fill in values locally.

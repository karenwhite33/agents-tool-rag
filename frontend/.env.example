# Backend API URL (no trailing slash). Local: http://localhost:8080
VITE_BACKEND_URL=https://your-backend-url.run.app

# API Key for authentication (same as backend API_KEY). Do not commit real keys.
# For no key exposure, use a BFF (see SECURITY.md).
VITE_API_KEY=your_api_key_here